# About / blog images (committed to git)

These files are served as **`/sawyer/avatar.jpg`** and **`/sawyer/og/*`** so the About page and writing cards work without depending on `sawyersweet.net`.

They match the originals that lived under **`public/images/`** on the pre-refactor magic-portfolio app (restored from git history). To swap art later, replace the binaries and keep the same paths (or update `app/about/page.tsx` and `heroImage` in `content/sawyer/posts/*.md`).
