# About / blog images (committed to git)

These files are served as **`/sawyer/avatar.jpg`** and **`/sawyer/og/*`** so the About page and writing cards work without depending on `sawyersweet.net` (those URLs were returning 404 from this environment).

Replace **`avatar.jpg`** and the **`og/*`** files with your real headshot and OG images when you have them; keep the same paths (or update `app/about/page.tsx` and `heroImage` in `content/sawyer/posts/*.md`).
